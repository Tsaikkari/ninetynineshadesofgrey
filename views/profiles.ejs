<%- include("header") -%>
<% posts.forEach(function(post){ %>
   <h1 class="profile-titles"> <%= post.title %> </h1>
      <p class="post-content"> <%= post.content.substring(0, 100) + " ..." %> 
         <a class="link" href="/posts/<%= post.title %>">Read More</a>
      </p>
<% }) %>
